"use client";
export default function SurveyListError({ reset }: { reset: () => void }) { return <div className="rounded-2xl bg-red-50 p-6 text-red-800"><h2 className="font-bold">Surveys could not be loaded</h2><p className="mt-2 text-sm">Try again without changing any data.</p><button onClick={reset} className="mt-4 rounded-xl border border-red-200 px-4 py-2 font-semibold">Retry</button></div>; }
