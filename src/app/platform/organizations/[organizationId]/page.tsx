import { notFound } from "next/navigation";
import { getPlatformOrganization } from "@/features/platform/server";

export default async function PlatformOrganizationPage({ params }: { params: Promise<{ organizationId: string }> }) { const { organizationId } = await params; const result = await getPlatformOrganization(organizationId); if (!result) notFound(); const o = result.organization; return <div className="grid gap-7"><header><p className="text-sm font-bold text-brand">{o.slug}</p><h1 className="mt-2 text-3xl font-bold">{result.context.profile.locale === "ar" ? o.name_ar : o.name_en}</h1><p className="mt-2 text-muted">{o.status} · created {new Date(o.created_at).toLocaleDateString(result.context.profile.locale === "ar" ? "ar-KW" : "en-KW", { timeZone: "Asia/Kuwait" })}</p></header><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{[["Members", o.member_count], ["Locations", o.location_count], ["Surveys", o.survey_count], ["Responses", o.response_count], ["Storage files", o.storage_objects]].map(([label, value]) => <article key={label} className="rounded-2xl border border-border bg-white p-5"><p className="text-sm text-muted">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></article>)}</div><p className="rounded-2xl bg-amber-50 p-4 text-sm text-amber-900">This operational view intentionally excludes customer answer text and does not expose mutation controls.</p></div>; }
